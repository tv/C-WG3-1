package yunikorn.core.packet.metainterfaces;

import yunikorn.core.packet.AudioSequence;
import yunikorn.core.packet.VideoFrame;

public interface DataPacketObservable<T extends VideoFrame, E extends AudioSequence,M extends DataPacketListener<? super T,? super E>> extends PacketObservable<M> {

	//public void addListener(M observer);
	//public void removeListener(M observer);
}
